name: sistem_araci
description: Gelişmiş Sistem ve Bellek Denetleyici
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Burayı 2.0.8 yaptık, hata veren yer burasıydı
  system_alert_window: ^2.0.8 
  permission_handler: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
